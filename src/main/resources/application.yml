rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem

spring:
  application:
    name: app

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update

  output:
    ansi:
      enabled: always

  profiles:
    default: development
